For Python:
- Write idiomatic, readable Python.
- Follow PEP 8 except where Ruff's formatter differs.
- Run `ruff format` after modifying Python.
- Run `ruff check` and fix all violations.
- Prefer readability over compact one-liners.